package com.dixond.display {	/*	*	Loads sequence of images and displays them as an animation at specified frame rate.	*	Specify whether the images are loaded as translatable, by passing an instance of the TranslationManager to the constructor	*	Hoping this will be as efficient as loading pre-made swfs. Should be same mem requirements, right...? 	*	Except we won't have the prog download thing... do we even get that from local files??	*	Pros: will allow us to do translatable anims with text, where translating the pre-made swf is difficult w/o authoring.	*	Cons: longer preload time. Any potential memory issues? Don't know that these would be worse than an swf tho...	*		*	@langversion ActionScript 3.0	*	@playerversion Flash 9.0	*	*	@author D	*	@since  6.19.2008	*/		import com.dixond.ia.TranslationManager;	import com.dixond.display.MediaLoader;	import com.dixond.ia.TranslatableMediaLoader;	public class AnimSprite extends Sprite {		//--------------------------------------		// PROTECTED VARIABLES		//--------------------------------------		//--------------------------------------		//  CONSTRUCTOR		//--------------------------------------		/**		*/		public function AnimSprite (aSourceDir:String, aTm:TranslationManager=null):void {		}					public function play(){					}				public function stop() {					}				private function loadImages()  {					}	}}